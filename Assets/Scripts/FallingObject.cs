using UnityEngine;

public class FallingObject : MonoBehaviour
{
    [SerializeField] float AfterTriggerDelay = 2f;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
